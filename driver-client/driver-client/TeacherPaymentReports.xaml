<Page x:Class="driver_client.TeacherPaymentReports"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="TeacherPaymentReports"
      Background="#0E1A2B">

    <Grid Margin="20">
        <!-- Background -->
        <Image Source="/picture/background.png"
               Stretch="UniformToFill"
               Opacity="0.06"
               Panel.ZIndex="0"/>

        <Grid Panel.ZIndex="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="💰 Payment Reports"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#00E0FF"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <!-- Summary Cards -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Total Earnings -->
                <Border Grid.Column="0" Background="#1B2D47" CornerRadius="10" Padding="15" Margin="0,0,10,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="💵" FontSize="28" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TotalEarningsText" Text="0 ₪" FontSize="24" FontWeight="Bold" 
                                   Foreground="#2ECC71" HorizontalAlignment="Center"/>
                        <TextBlock Text="Total Earnings" FontSize="12" Foreground="LightGray" 
                                   HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- This Month -->
                <Border Grid.Column="1" Background="#1B2D47" CornerRadius="10" Padding="15" Margin="0,0,10,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="📅" FontSize="28" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="ThisMonthText" Text="0 ₪" FontSize="24" FontWeight="Bold" 
                                   Foreground="#00E0FF" HorizontalAlignment="Center"/>
                        <TextBlock Text="This Month" FontSize="12" Foreground="LightGray" 
                                   HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Pending -->
                <Border Grid.Column="2" Background="#1B2D47" CornerRadius="10" Padding="15" Margin="0,0,10,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="⏳" FontSize="28" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="PendingText" Text="0 ₪" FontSize="24" FontWeight="Bold" 
                                   Foreground="#F39C12" HorizontalAlignment="Center"/>
                        <TextBlock Text="Pending" FontSize="12" Foreground="LightGray" 
                                   HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Students with Debt -->
                <Border Grid.Column="3" Background="#1B2D47" CornerRadius="10" Padding="15">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="👥" FontSize="28" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="StudentsWithDebtText" Text="0" FontSize="24" FontWeight="Bold" 
                                   Foreground="#E74C3C" HorizontalAlignment="Center"/>
                        <TextBlock Text="Students with Debt" FontSize="12" Foreground="LightGray" 
                                   HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Payment List -->
            <Border Grid.Row="2" Background="#1B2D47" CornerRadius="10" Padding="15">
                <DockPanel>
                    <TextBlock Text="Recent Payments"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="#00E0FF"
                               DockPanel.Dock="Top"
                               Margin="0,0,0,15"/>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="PaymentsPanel"/>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <!-- Back Button -->
            <Button Grid.Row="3"
                    Content="← Back"
                    Width="100" Height="40"
                    Margin="0,15,0,0"
                    HorizontalAlignment="Left"
                    Background="#00E0FF"
                    Foreground="Black"
                    FontWeight="Bold"
                    BorderBrush="#00C0CC"
                    Cursor="Hand"
                    Click="Back_Click"/>
        </Grid>

        <Frame x:Name="page" Panel.ZIndex="100" Margin="-20,-20,-20,-20"/>
    </Grid>
</Page>
