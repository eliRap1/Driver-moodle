<Page x:Class="driver_client.AllStudents"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="650" d:DesignWidth="960"
      Title="AllStudents"
      Background="#0E1A2B">

    <Grid>
        <Image Source="/picture/background1.png"
               Stretch="UniformToFill"
               Opacity="0.05"
               Panel.ZIndex="0"/>

        <Grid Panel.ZIndex="1" Margin="24" MaxWidth="1080" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Content="Back"
                        Style="{StaticResource PrimaryButton}"
                        Width="96"
                        Height="38"
                        FontSize="14"
                        Margin="0"
                        Click="BackButton_Click"/>

                <StackPanel Grid.Column="1" Margin="18,0,0,0">
                    <TextBlock Text="My students"
                               FontSize="30"
                               FontWeight="Bold"
                               Foreground="#00E0FF"/>
                    <TextBlock Text="Confirm new students and keep contact details close while teaching."
                               FontSize="14"
                               Foreground="#AAB7C8"
                               TextWrapping="Wrap"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>

            <WrapPanel Grid.Row="1" Margin="0,0,0,12">
                <Border Style="{StaticResource CardBorder}" Width="170" Margin="0,0,10,10">
                    <StackPanel>
                        <TextBlock Text="Total" Foreground="#AAB7C8"/>
                        <TextBlock x:Name="TotalStudentsText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#00E0FF"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Width="170" Margin="0,0,10,10">
                    <StackPanel>
                        <TextBlock Text="Waiting" Foreground="#AAB7C8"/>
                        <TextBlock x:Name="PendingStudentsText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#F39C12"/>
                    </StackPanel>
                </Border>
            </WrapPanel>

            <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
                <Grid>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="StudentListPanel" Margin="10"/>
                    </ScrollViewer>

                    <StackPanel x:Name="EmptyState"
                                Visibility="Collapsed"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Margin="24">
                        <TextBlock Text="No students found"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="#AAB7C8"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Students assigned to you will appear here when the service returns them."
                                   FontSize="13"
                                   Foreground="#7F8DA3"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Frame x:Name="page" Panel.ZIndex="100"/>
    </Grid>
</Page>
