<Page x:Class="driver_client.CalendarTeacher"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="CalendarTeacher" Background="AliceBlue">
    

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image Source="/picture/background.png"
               Stretch="UniformToFill"
               Opacity="0.7"
               Grid.RowSpan="3" Margin="-21,-20,-24,-21"/>
        <TextBlock Text="Set Your Availability" FontSize="24" FontWeight="Bold" Foreground="#2A239A" HorizontalAlignment="Center" Margin="0,10"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,20" HorizontalAlignment="Center" VerticalAlignment="Top" Width="400">

            <!-- Weekdays Selection -->
            <GroupBox Header="Available Days" Margin="0,10">
                <WrapPanel>
                    <CheckBox Content="Monday" x:Name="MondayCheck"/>
                    <CheckBox Content="Tuesday" x:Name="TuesdayCheck"/>
                    <CheckBox Content="Wednesday" x:Name="WednesdayCheck"/>
                    <CheckBox Content="Thursday" x:Name="ThursdayCheck"/>
                    <CheckBox Content="Friday" x:Name="FridayCheck"/>
                    <CheckBox Content="Saturday" x:Name="SaturdayCheck"/>
                    <CheckBox Content="Sunday" x:Name="SundayCheck"/>
                </WrapPanel>
            </GroupBox>

            <!-- Time Selection -->
            <GroupBox Header="Working Hours" Margin="0,10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="From:" VerticalAlignment="Center"/>
                    <ComboBox x:Name="StartHour" Width="100"/>
                    <TextBlock Text="To:" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    <ComboBox x:Name="EndHour" Width="100"/>
                </StackPanel>
            </GroupBox>

            <!-- Calendar for Unavailable Days -->
            <GroupBox Header="Unavailable Dates" Margin="0,10">
                <StackPanel>
                    <Calendar x:Name="UnavailableCalendar" SelectionMode="MultipleRange"/>
                    <Button Content="Clear Selection" Width="120" Margin="0,5" Click="ClearSelection_Click"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <!-- Save Button -->
        <Button Content="Save Availability" Grid.Row="2" Width="200" Height="40" HorizontalAlignment="Center" Margin="0,10" Click="SaveAvailability_Click"/>
    </Grid>
</Page>
