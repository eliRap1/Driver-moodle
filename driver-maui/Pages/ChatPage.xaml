<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="driver_maui.Pages.ChatPage"
             Title="Chat">
    <Grid RowDefinitions="Auto,*,Auto" Padding="10" RowSpacing="8">
        <Button Grid.Row="0" Text="Back" Clicked="Back_Click"
                HorizontalOptions="Start" WidthRequest="80"/>
        <CollectionView Grid.Row="1" x:Name="MessagesList">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout HorizontalOptions="{Binding Alignment}" Margin="0,3">
                        <Border Padding="10" BackgroundColor="{Binding BgColor}"
                               HorizontalOptions="{Binding Alignment}"><Border.StrokeShape><RoundRectangle CornerRadius="8"/></Border.StrokeShape>
                            <VerticalStackLayout Spacing="2">
                                <Label Text="{Binding Username}" FontAttributes="Bold" FontSize="12"
                                       TextColor="{Binding NameColor}"/>
                                <Label Text="{Binding Message}"/>
                            </VerticalStackLayout>
                        </Border>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Grid Grid.Row="2" ColumnDefinitions="*,Auto" ColumnSpacing="8">
            <Entry Grid.Column="0" x:Name="MessageEntry" Placeholder="Type a message..."
                   ReturnCommand="{Binding SendCommand}"/>
            <Button Grid.Column="1" Text="Send" Clicked="Send_Click" BackgroundColor="#2196F3" WidthRequest="80"/>
        </Grid>
    </Grid>
</ContentPage>
